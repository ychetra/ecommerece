import 'package:flutter/material.dart';

// Font Sizes
const double smallFontSize = 12.0;
const double mediumFontSize = 16.0;
const double largeFontSize = 20.0;
const double extraLargeFontSize = 24.0;

// Padding Sizes
const EdgeInsetsGeometry smallPadding = EdgeInsets.all(8.0);
const EdgeInsetsGeometry mediumPadding = EdgeInsets.all(16.0);
const EdgeInsetsGeometry largePadding = EdgeInsets.all(24.0);

// Margin Sizes
const EdgeInsetsGeometry smallMargin = EdgeInsets.all(8.0);
const EdgeInsetsGeometry mediumMargin = EdgeInsets.all(16.0);
const EdgeInsetsGeometry largeMargin = EdgeInsets.all(24.0);

// Button Sizes
const double buttonHeight = 48.0;
const double buttonWidth = 150.0;

// Icon Sizes
const double smallIconSize = 20.0;
const double mediumIconSize = 24.0;
const double largeIconSize = 30.0;

// Border Radius
const BorderRadiusGeometry smallBorderRadius =
    BorderRadius.all(Radius.circular(8.0));
const BorderRadiusGeometry mediumBorderRadius =
    BorderRadius.all(Radius.circular(16.0));
const BorderRadiusGeometry largeBorderRadius =
    BorderRadius.all(Radius.circular(24.0));

// Card Sizes
const double cardHeight = 200.0;
const double cardWidth = 300.0;

// Container Sizes
const double containerHeight = 100.0;
const double containerWidth = 150.0;

// SizedBox Sizes
const SizedBox smallSizedBox = SizedBox(height: 8.0);
const SizedBox mediumSizedBox = SizedBox(height: 16.0);
const SizedBox largeSizedBox = SizedBox(height: 24.0);
const SizedBox spaceSizedBox = SizedBox(height: 32.0);
